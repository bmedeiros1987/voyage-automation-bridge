# Required secrets: configure in Render Environment, never commit real values.
TINYFISH_API_KEY=
BRIDGE_API_KEY=

# Comma-separated HTTPS host allowlist for browser automation.
ALLOWED_HOSTS=console.cloud.google.com,news.ycombinator.com

# Optional service tuning.
RATE_LIMIT_PER_MINUTE=20
PORT=3000
