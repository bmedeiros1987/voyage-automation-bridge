{
  "name": "voyage-automation-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal authenticated bridge between Voyage tooling and TinyFish Agent API.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  }
}
